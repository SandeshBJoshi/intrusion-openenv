name: intrusion-openenv

endpoints:
  reset: /reset
  step: /step
  state: /state

methods:
  reset: POST
  step: POST
  state: GET

actions:
  - Normal
  - BruteForce
  - DDoS
  - PortScan
  - DataExfiltration
  - MultiAttack